version: "2.0"

intents:
  - pipe
  - layers
  - tiers

responses:
  utter_greet:
  - text: "Hey! How are you?"

entities:
  - COMPONENT
  - PATTERN

session_config:
  session_expiration_time: 60
  carry_over_slots_to_new_session: true
